info:
  name: Api_porID
  type: http

http:
  method: GET
  url: http://localhost:3000/api/clients/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
